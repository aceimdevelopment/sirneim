/ Ingrese los datos a continuación:
Confirmación de Datos

=form_tag :action => "paso2_guardar" do 
  -if @usuario.errors.any? || @inscripcion.errors.any?
    %p
      %div#error-explanation
        %b
          =pluralize(@usuario.errors.count+@datos_estudiante.errors.count, "error") 
          en el formulario impiden que éste usuario sea registrado:
        %ul
          -@usuario.errors.full_messages.each do |msg|
            %li
              =msg
          -@datos_estudiante.errors.full_messages.each do |msg|
            %li
              =msg
  %fieldset
    %legend Datos personales
    %p
      %label
        Cédula de identidad
      =@usuario.ci
    %p
      %label
        Nombre
      =@usuario.nombre_completo
    %p
      %label
        Forma de pago
      = @preinscripcion.tipo_forma_pago.descripcion
      = hidden_field_tag :tipo_forma_pago_id, @preinscripcion.tipo_forma_pago.id
      
      =#collection_select :inscripcion, :tipo_forma_pago_id, @preinscripcion.tipo_forma_pago, :id, :descripcion
    %p
      %label
        Grupo
      = @preinscripcion.grupo.nombre
      = hidden_field_tag :grupo_id, @preinscripcion.grupo.id

      =#collection_select :inscripcion, :grupo_id, @preinscripcion.grupo, :id, :nombre
    %p
      %label
        Cohorte
      = "#{@preinscripcion.cohorte.nombre} - #{@preinscripcion.cohorte.id}"
      = hidden_field_tag :cohorte_id, @preinscripcion.cohorte.id

      =#collection_select :inscripcion, :cohorte_id, @preinscripcion.cohorte, :id, :nombre

  / %p 
  /   %span.req *  
  /   Campos Obligatorios
  %p
    =submit_tag "Siguiente", :confirm => "¿Está seguro de enviar los datos?"
%br/    