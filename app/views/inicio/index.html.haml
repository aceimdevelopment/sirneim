/ %h3 Información Importante:
-#if ParametroGeneral.inscripcion_abierta
=#button_to "PresInscribirse", {:action => "paso1", :controller => "inscripcion"},:class => 'btn'
=#button_to "Registrarse", {:action => "registrar"}, :class => 'btn'

/ .well{:style=>"overflow-y: scroll;height: 200px;"}
=#@content.html_safe



-if ParametroGeneral.inscripcion_abierta
	=#button_to "PresInscribirse", {:action => "paso1", :controller => "inscripcion"},:class => 'btn'
	.well.well-small
		%h4.center
			%b Para Inscribrir un Diplomado debe ingresar en su cuenta o registrarse
		%div
			=button_to "Registrarse", {:action => "registrar"}, :class => 'btn btn-primary btn-block btn-large center'

.tabbable.tabs-left
	%ul.nav.nav-tabs
		-@diplomados.each do |diplomado|
			%li
				=link_to diplomado.id, "##{diplomado.id}", "data-toggle"=> "tab"
	.tab-content
		-@diplomados.each do |diplomado|
			.tab-pane{:id=>"#{diplomado.id}"}

				#accordionMenu.accordion
					.accordion-group
						.nav-header
							%a.accordion-toggle{"data-parent" => "#accordionMenu", "data-toggle" => "collapse", :href => "##{diplomado.id}_caracteristica"}
								.nav-header
									Características
					.accordion-body.collapse{:id=>"#{diplomado.id}_caracteristica"}
						.accordion-inner
							%b Objetivo:
							%p= diplomado.objetivo
							%b Destinatario:
							%p= diplomado.destinatario
							%p
								%b Modalidad:
								= diplomado.modalidad
							%p
								%b Duración:
								= diplomado.duracion
							%b Horario:
							%p= diplomado.horario
							%b Descripción
							%p= diplomado.descripcion_completa
					.accordion-group
						.nav-header
							%a.accordion-toggle{"data-parent" => "#accordionMenu", "data-toggle" => "collapse", :href => "##{diplomado.id}_modulos"}
								.nav-header
									Módulos
					.accordion-body.collapse{:id=>"#{diplomado.id}_modulos"}
						.accordion-inner

							-diplomado.modulos.each do |modulo|
								%dl.well.dl-horizontal
									%dt= "#{modulo.descripcion_completa}"
									%dd
										-modulo.temas.each do |tema|
											%li
												%p= tema.descripcion_completa

					.accordion-group
						.nav-header
							%a.accordion-toggle{"data-parent" => "#accordionMenu", "data-toggle" => "collapse", :href => "##{diplomado.id}_docentes"}
								.nav-header
									Grupo Docente
					.accordion-body.collapse{:id=>"#{diplomado.id}_docentes"}
						.accordion-inner

					.accordion-group
						.nav-header
							%a.accordion-toggle{"data-parent" => "#accordionMenu", "data-toggle" => "collapse", :href => "##{diplomado.id}_inscripcion"}
								.nav-header
									Inscripciones
					.accordion-body.collapse{:id=>"#{diplomado.id}_inscripcion"}
						.accordion-inner
							%b Datos de la cuenta:
							%p Banco de Venezuela
							%p Cuenta Corriente Nro. 0102-0140-3400-0442-6884  
							%p A nombre de FUNDEIM
							%p
								%b Inversión:
								Bs. 7.600,ºº (pago único)
							%p
								%b Fecha de inscripciones:
								del 16 al 19 de septiembre
							%p
								%b Horario de inscripciones:
								de 3:00  a 7:00 pm. Escuela de Idiomas Modernos, ofic. FUNDEIM, Cursos de Extensión de Idiomas.  Galpón Nº 7; frente a la Facultad de Farmacia.
							%p
								%b Nota: 
								La Escuela de Idiomas Modernos se reserva el derecho de postergar el inicio de clases o anular el Diplomado si el número de inscritos no llega al mínimo establecido.
