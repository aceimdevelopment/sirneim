.navbar.navbar-fluid.navbar-static-top{:role=>"navigation"}
  .navbar-inner
    .container-fluid
      %a.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      -if session[:administrador]
        %a.brand{:href => "principal_admin", :style => ""} Aceim
      .container-fluid.nav-collapse
        %ul.nav
          -if session[:administrador]
            %li= link_to "Estudiantes", "estudiante"

            %li= link_to "Instructores", "instructor"
            %li= link_to "Link3", "/path3"
            = drop_down "Products" do
              = menu_item "Latest", "#"   
              = menu_item "Top Sellers", "#"  
              = drop_down_divider
              = menu_item "Discount Items", "#"
        =# menu_group :pull => :right do
        -if session[:usuario]
          = drop_down "#{session[:usuario].nombre_completo}" do
            %li
              =link_to "Cambiar Contraseña", :controller=>"usuario", :action=>"contrasena", :controlador=>controller_name, :accion=>action_name 
            %li
              =link_to "Cambiar Datos Personales", :controller=>"usuario", :action=>"modificar", :controlador=>controller_name, :accion=>action_name 
            = drop_down_divider
            = menu_item "Cerrar sesión", "inicio/cerrar_sesion"
        -else
          =render 'layouts/cargar_sesion'