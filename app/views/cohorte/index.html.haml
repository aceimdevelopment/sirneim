-if @cohortes.count > 0
	-@cohortes.each do |cohorte|
		%h2
			=cohorte.descripcion
-else
	%h2 Sin Cohortes

= link_to "cohorte/nuevo", {:class => 'btn btn-primary'} do
	%i.icon-plus.icon-white
	Agregar Cohorte
